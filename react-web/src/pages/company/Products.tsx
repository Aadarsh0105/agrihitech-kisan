import { useEffect, useState } from 'react';
import { Edit3, Package, Plus, Trash2 } from 'lucide-react';
import { Button } from '../../components/admin/ui/Button';
import { Field, Input, Select, Textarea } from '../../components/admin/ui/Input';
import { ConfirmDialog, Modal } from '../../components/admin/ui/Modal';
import { getPublicCategories, type PublicCategory } from '../../services/category.service';
import { createCompanyProduct, deleteCompanyProduct, getCompanyBrands, getCompanyProducts, updateCompanyProduct, type CompanyBrandApi, type CompanyProductApi, type CompanyProductDraft } from '../../services/company.service';
import { Empty, Header } from './Brands';

const blank: CompanyProductDraft = { name: '', brand: '', category: '', description: '', price: 0, quantity: 0, unit: '', images: [] };
export function CompanyProducts() {
  const [products, setProducts] = useState<CompanyProductApi[]>([]);
  const [brands, setBrands] = useState<CompanyBrandApi[]>([]);
  const [categories, setCategories] = useState<PublicCategory[]>([]);
  const [draft, setDraft] = useState(blank);
  const [editing, setEditing] = useState<CompanyProductApi | null>(null);
  const [remove, setRemove] = useState<CompanyProductApi | null>(null);
  const [open, setOpen] = useState(false);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');
  const load = () => Promise.all([getCompanyProducts(), getCompanyBrands(), getPublicCategories()]).then(([items, brandRows, categoryRows]) => { setProducts(items); setBrands(brandRows); setCategories(categoryRows); }).catch((reason) => setError(reason.response?.data?.message || reason.message)).finally(() => setLoading(false));
  useEffect(() => { load(); }, []);
  const create = () => { setEditing(null); setDraft(blank); setOpen(true); };
  const edit = (item: CompanyProductApi) => { setEditing(item); setDraft({ name: item.name, brand: item.brand?.[0]?._id || '', category: item.category?._id || '', description: item.description || '', price: item.price || 0, quantity: item.quantity || 0, unit: item.unit || '', images: [] }); setOpen(true); };
  const save = async () => { setSaving(true); setError(''); try { if (editing) await updateCompanyProduct(editing._id, draft); else await createCompanyProduct(draft); setOpen(false); await load(); } catch (reason: any) { setError(reason.response?.data?.message || reason.message); } finally { setSaving(false); } };
  return <div className="space-y-6"><Header title="My Products" description="Products owned by your company, including items created under it by dealers or admins." action={<Button variant="primary" onClick={create}><Plus className="h-4 w-4" />Add product</Button>} />{error ? <p className="rounded-xl bg-destructive/10 p-3 text-sm text-destructive">{error}</p> : null}{loading ? <div className="h-64 animate-pulse rounded-2xl bg-muted" /> : products.length ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{products.map((product) => <article key={product._id} className="overflow-hidden rounded-2xl border border-border bg-card shadow-soft"><div className="grid h-40 place-items-center bg-muted/50">{product.images?.[0]?.url ? <img src={product.images[0].url} alt={product.name} className="h-full w-full object-contain p-3" /> : <Package className="h-10 w-10 text-primary/50" />}</div><div className="p-4"><p className="text-xs font-semibold uppercase text-primary">{product.brand?.map((row) => row.name).join(', ')}</p><h2 className="mt-1 font-display font-bold">{product.name}</h2><p className="mt-2 font-semibold">INR {(product.price || 0).toLocaleString('en-IN')}</p><p className="mt-1 text-xs text-muted-foreground">Created by {product.createdBy?.role || 'COMPANY'}</p><div className="mt-4 flex gap-2 border-t border-border pt-3"><Button size="sm" className="flex-1" onClick={() => edit(product)}><Edit3 className="h-4 w-4" />Edit</Button><Button size="sm" variant="outlineDanger" onClick={() => setRemove(product)}><Trash2 className="h-4 w-4" /></Button></div></div></article>)}</div> : <Empty text="No company products added yet." />}<Modal open={open} onClose={() => setOpen(false)} title={editing ? 'Edit company product' : 'Add company product'} size="lg" footer={<><Button onClick={() => setOpen(false)}>Cancel</Button><Button variant="primary" disabled={saving || !draft.name || !draft.brand || !draft.category} onClick={save}>{saving ? 'Saving...' : editing ? 'Save changes' : 'Create product'}</Button></>}><div className="grid gap-4 sm:grid-cols-2"><Field label="Product name" required><Input value={draft.name} onChange={(e) => setDraft((old) => ({ ...old, name: e.target.value }))} /></Field><Field label="Brand" required><Select value={draft.brand} onChange={(e) => setDraft((old) => ({ ...old, brand: e.target.value }))}><option value="">Select existing brand</option>{brands.map((brand) => <option key={brand._id} value={brand._id}>{brand.name}</option>)}</Select></Field><Field label="Category" required><Select value={draft.category} onChange={(e) => setDraft((old) => ({ ...old, category: e.target.value }))}><option value="">Select category</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</Select></Field><Field label="Unit"><Input value={draft.unit} onChange={(e) => setDraft((old) => ({ ...old, unit: e.target.value }))} /></Field><Field label="Price"><Input type="number" min="0" value={draft.price} onChange={(e) => setDraft((old) => ({ ...old, price: Number(e.target.value) }))} /></Field><Field label="Quantity"><Input type="number" min="0" value={draft.quantity} onChange={(e) => setDraft((old) => ({ ...old, quantity: Number(e.target.value) }))} /></Field><Field label="Images" className="sm:col-span-2"><Input type="file" multiple accept="image/*" onChange={(e) => setDraft((old) => ({ ...old, images: Array.from(e.target.files || []) }))} /></Field><Field label="Description" className="sm:col-span-2"><Textarea value={draft.description} onChange={(e) => setDraft((old) => ({ ...old, description: e.target.value }))} /></Field></div></Modal><ConfirmDialog open={Boolean(remove)} onClose={() => setRemove(null)} onConfirm={async () => { if (remove) { await deleteCompanyProduct(remove._id); await load(); } }} title="Delete product" message="This company product will be permanently removed." /></div>;
}
