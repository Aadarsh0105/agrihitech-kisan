import { CreditCardIcon, IndianRupeeIcon, ReceiptIcon } from 'lucide-react';
import { PageHeader } from '../../components/admin/shared/PageHeader';
import { Card, CardHeader } from '../../components/admin/ui/Card';
import { Badge } from '../../components/admin/ui/Badge';

const payments = [{ id: 'PAY-2048', account: 'Krishi Traders', plan: 'Growth', amount: 'INR 1,499', date: '18 Aug 2026', status: 'Paid' }, { id: 'PAY-2047', account: 'Agro Point', plan: 'Starter', amount: 'INR 499', date: '17 Aug 2026', status: 'Pending' }];
export function Payments() { return <><PageHeader title="Payments" description="Monitor subscription transactions and payment status." /><section className="grid gap-3 sm:grid-cols-3"><Metric icon={IndianRupeeIcon} label="Collected" value="INR 1,499" /><Metric icon={CreditCardIcon} label="Transactions" value={String(payments.length)} /><Metric icon={ReceiptIcon} label="Pending" value={String(payments.filter((row) => row.status === 'Pending').length)} /></section><Card><CardHeader title="Recent transactions" description="Latest subscription payment activity" /><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/50 text-xs uppercase text-muted-foreground"><tr><th className="px-5 py-3">Payment</th><th className="px-5 py-3">Account</th><th className="px-5 py-3">Plan</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Date</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-border">{payments.map((row) => <tr key={row.id}><td className="px-5 py-4 font-medium">{row.id}</td><td className="px-5 py-4">{row.account}</td><td className="px-5 py-4">{row.plan}</td><td className="px-5 py-4 font-semibold">{row.amount}</td><td className="px-5 py-4 text-muted-foreground">{row.date}</td><td className="px-5 py-4"><Badge tone={row.status === 'Paid' ? 'success' : 'warning'}>{row.status}</Badge></td></tr>)}</tbody></table></div></Card></>; }
function Metric({ icon: Icon, label, value }: { icon: typeof CreditCardIcon; label: string; value: string }) { return <Card className="p-5"><Icon className="h-5 w-5 text-primary" /><p className="mt-4 text-xs text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></Card>; }
