import { BarChart3Icon, DownloadIcon, FileSpreadsheetIcon } from 'lucide-react';
import { PageHeader } from '../../components/admin/shared/PageHeader';
import { Button } from '../../components/admin/ui/Button';
import { Card } from '../../components/admin/ui/Card';

const reports = [{ title: 'Catalogue report', description: 'Categories, brands, products and catalogue status.' }, { title: 'Dealer network report', description: 'Dealer registrations, requests and verification status.' }, { title: 'Subscription report', description: 'Plan adoption, active subscriptions and renewals.' }, { title: 'Platform activity report', description: 'User growth and content activity summary.' }];
export function Reports() { return <><PageHeader title="Reports" description="Generate operational summaries for the Agri HiTech platform." /><div className="grid gap-4 md:grid-cols-2">{reports.map((report) => <Card key={report.title} className="p-5"><div className="flex items-start gap-4"><span className="grid h-11 w-11 place-items-center rounded-lg bg-primary-subtle text-primary"><FileSpreadsheetIcon className="h-5 w-5" /></span><div className="min-w-0 flex-1"><h2 className="font-semibold">{report.title}</h2><p className="mt-1 text-sm text-muted-foreground">{report.description}</p><Button className="mt-4" size="sm"><DownloadIcon className="h-4 w-4" />Export report</Button></div></div></Card>)}</div><Card className="p-5"><div className="flex items-center gap-3"><BarChart3Icon className="h-5 w-5 text-primary" /><div><h2 className="font-semibold">Reporting overview</h2><p className="text-sm text-muted-foreground">Choose a report above to prepare an export.</p></div></div></Card></>; }
